import { useEffect,useState } from "react";import api from "../services/api";import ReviewTable from "../components/ReviewTable";import EmptyState from "../components/EmptyState";
const ReviewHistory=()=>{const[reviews,setReviews]=useState([]);const[language,setLanguage]=useState("all");const[search,setSearch]=useState("");const load=async()=>{const{data}=await api.get(`/reviews/history?language=${language}&search=${encodeURIComponent(search)}`);setReviews(data)};useEffect(()=>{load()},[language]);return <main className="max-w-7xl mx-auto px-4 py-10"><h1 className="text-3xl font-bold mb-6">Review History</h1><div className="card p-5 mb-6 grid md:grid-cols-3 gap-4"><input className="input md:col-span-2" placeholder="Search by project name" value={search} onChange={(e)=>setSearch(e.target.value)}/><div className="flex gap-3"><select className="input" value={language} onChange={(e)=>setLanguage(e.target.value)}><option value="all">All</option><option value="javascript">JavaScript</option><option value="python">Python</option><option value="java">Java</option></select><button onClick={load} className="btn-primary">Search</button></div></div>{reviews.length?<ReviewTable reviews={reviews}/>:<EmptyState title="No reviews yet" message="Run your first code analysis to see history here."/>}</main>};export default ReviewHistory;
