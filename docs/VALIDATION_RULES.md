# Validation Rules

Frontend validates required fields, GitHub URLs, file extensions, and file size. Backend validates JWT tokens, request bodies, roles, file size/type, and repository limits. AI service rejects empty code, unsupported languages, and oversized input.
